runtime: python27
api_version: 1
threadsafe: true

libraries:
- name: flask
  version: "0.12"

handlers:
- url: /.*
  script: main.app

instance_class: B1
basic_scaling:
  idle_timeout: 5m
  max_instances: 1